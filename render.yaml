services:
  - type: web
    name: arogya-mitra
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app_no_aws:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
